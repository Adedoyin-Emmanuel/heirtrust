import React from 'react'

const AppWaitlist = ():JSX.Element =>
{
    return (
        <React.Fragment>
            <section className="container-fluid">
                <h1 className="fw-bold text-capitalize text-center">join our waitlist </h1>
                <p className="brand-small-text-3 text-dark">get exclusive promotions & updates straight to your inbox. No spam</p>
            </section>
        </React.Fragment>
    );
}

export default AppWaitlist;